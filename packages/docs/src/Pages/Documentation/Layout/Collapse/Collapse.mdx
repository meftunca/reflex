---
name: Collapse
menu: Layout
route: /documentation/layout/collapse
---

import { List, Button, Collapse } from "@re-flex/ui";
import CollapseSimple from "./";

## Basic usage

<CollapseSimple />

```js live=true
function Example() {
  const [open, setOpen] = React.useState(false);
  return (
    <div>
      <Button onClick={() => setOpen(!open)}>Toggle Collapse</Button>
      <Collapse open={open} starterHeight={20}>
        <Box bgColor="#f0f0f0" p={40} radius={6}>
          <Text>Collapse Content</Text>
        </Box>
      </Collapse>
    </div>
  );
}
```

## Properties

<Props of={Collapse} />
