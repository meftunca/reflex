import DefaultTheme from "../../../utils/theme/defaultTheme";
declare const CheckboxStyled: ({ prefix, palette, transitions, }: typeof DefaultTheme) => import("@emotion/utils").SerializedStyles;
export default CheckboxStyled;
